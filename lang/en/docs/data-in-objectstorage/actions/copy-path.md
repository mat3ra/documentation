# Copy Path of Files

The path of the file, within the [directory structure of the cluster]({{ resources_url }}/infrastructure/clusters/directories/) where it is [stored]({{ resources_url }}/infrastructure/storage/), can be **copied to the clipboard** through the corresponding `Copy Path` [dropdown button](../../entities-general/ui/explorer.md#actions-dropdown) <i class="zmdi zmdi-copy zmdi-hc-border"></i> under [Files Explorer](../ui/explorer.md).

Upon its copying, this path can be conveniently used in the [Command Line Interface]({{ cli_url }}/cli/overview/) or [Remote Desktop]({{ cli_url }}/remote-connection/remote-desktop/) environments for example.

## Animation

In the following animation, we demonstrate how to copy the path of a file to the clipboard. We then paste it in the [Web Terminal]({{ cli_url }}/remote-connection/web-terminal/) with the help of the [Remote-connection Sidebar]({{ cli_url }}/remote-connection/actions/sidebar/). 

<img data-gifffer="/images/data-in-objectstorage/copy-path.gif">
